<% if (title) { %>
    <h1>
        <%= title %>
    </h1>

    <% } %>


        <section class="management-appointments">
            <h2>Agenda</h2>
            <div class="filters">

                <label for="filterBydateid">Filtrar por fecha: <input type="date" id="filterBydateid"
                        class="filterBydate" name="filterBydate"></label>
            </div>

            <div class="management-appointments-table">

            </div>
        </section>

        <%- messages() %>
            <% if (errors) { %>
                <ul class="notice">
                    <% errors.array().forEach(error=> { %>
                        <li>
                            <%= error.msg %>
                        </li>
                        <% }) %>
                </ul>
                <% } %>

                    <!-- <script src="../../js/scheduleNotAvailableForm.js"></script> -->
                    <script type="module" src="../../js/managementClientReservations.mjs"></script>